#include <stdio.h>
#include <math.h>

int main()
{
int x, y, mas;
printf("input point coordinates:\n");
scanf("%d %d", &x, &y);
mas = ((x*x)+(y*y));
switch (mas)
{
case 1:
printf("There is can be an 1-r circle.");
break;
default:
printf("There is no way to be 1-r circle.");
break;
}
return 0;
}
